package util;

import javax.swing.*;

public class UIHelper {

    // Maximizes a JFrame to full screen
    public static void maximize(JFrame frame) {
        frame.setExtendedState(JFrame.MAXIMIZED_BOTH);
    }
}